server:
  port: 8083

spring:
  security:
    jwt:
      secret: YOUR_SECRET_KEY_CHANGE_ME_TO_SOMETHING_LONG_AND_RANDOM

# Test flow:
# 1. curl -X POST http://localhost:8083/auth/login \
#      -H "Content-Type: application/json" \
#      -d '{"username":"alice"}'
#    -> returns { "token": "..." }
#
# 2. curl http://localhost:8083/secure \
#      -H "Authorization: Bearer <token-from-step-1>"
#    -> "Hello alice, this is a JWT-secured endpoint"
