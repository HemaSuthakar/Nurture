spring.application.name=example-service

# Default port - override with --server.port=8082 --instance.id=example-service-2
# when starting a second instance for load-balancing demos.
server.port=8081
instance.id=example-service-1

management.endpoints.web.exposure.include=health,info
